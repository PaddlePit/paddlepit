# PaddlePit API

FastAPI backend for the PaddlePit booking system.

## Run Locally

Install dependencies:

```bash
uv sync
```

Start the development server:

```bash
uv run fastapi dev main.py
```

API:

```
http://localhost:8000
```

API Docs:

```
http://localhost:8000/docs
```
